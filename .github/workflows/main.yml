name: Build & Publish

# on:
#   push:
#     tags:
#       - 'v*'

on:
  push:
    branches:
      - 'master'

jobs:
  install-and-build:
    name: 🛠️ Install, Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🗜️ Checkout
        uses: actions/checkout@v7

      - name: 🗜️ Setup node version
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: 🗜️ Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: 🛠️ Set git info
        uses: ./.github/actions/git-info

      - name: 🛠️ Install
        uses: ./.github/actions/cached-deps

      - name: 🧹 Lint
        run: bun run lint

      - name: 🧪 Test
        run: bun run test

      - name: 📦 Build demo app
        run: bun run build --base-href https://celtian.github.io/demo-angular-material-rxdb/

      - name: 🛠️ Copy index.html to 404.html
        run: cp ./dist/demo-angular-material-rxdb/browser/index.html ./dist/demo-angular-material-rxdb/browser/404.html

      - name: 🚀 Deploy Github Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          publish_branch: gh-pages
          publish_dir: ./dist/demo-angular-material-rxdb/browser
